import { V1ITransforamtionOptions } from "./types";
/**
 * Things dropped
 * - responsive_width
 * - config().dpr
 * - SSL Detected
 * - Provisioning API
 * - Magical configuration auto-mapping (everything has to be explicit)
 * - Signatures
 * - Secure is default true
 * @param transformationOptions
 */
export declare function generateTransformationString(transformationOptions: V1ITransforamtionOptions): string;
