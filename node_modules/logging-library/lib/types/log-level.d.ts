/**
 * Enum representing each log level.
 */
export declare enum LogLevel {
    VERBOSE = 10,
    DEBUG = 20,
    INFO = 30,
    WARNING = 40,
    ERROR = 50,
    CRITICAL = 60
}
/**
 * Get the name for a given LogLevel.
 * @param level LogLevel enum entry
 * @returns Name of the given level
 * @hidden
 */
export declare function getLogLevelName(level: LogLevel): keyof typeof LogLevel;
